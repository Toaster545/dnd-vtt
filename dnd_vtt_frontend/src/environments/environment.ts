export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000/api',
  // Only used for Supabase Realtime (live token sync) — not for CRUD
  supabaseUrl: 'YOUR_SUPABASE_URL',
  supabaseKey: 'YOUR_SUPABASE_ANON_KEY',
};
